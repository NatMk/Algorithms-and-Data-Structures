#include<stdio.h>

int main(void)
{
    int A_num , X_num, A_count, X_count;
    int A_int, X_int, num = -999999999;
    
    printf("Enter No_ of intergers in sequence A and sequence X space separted: ");
    scanf("%d %d",&A_num,&X_num);
    
    while(A_num != A_count)
    {
	printf("Enter the integer for Sequence A: ");
	scanf("%d",&A_int);
	A_count++;
	if (A_int == num)
	   break;	
    }

    while(X_num != X_count) 
    {
        printf("Enter the integer for Sequence X: ");
        scanf("%d",&X_int);
	X_count++;
        if (X_int == num)
           break;
     }







}
